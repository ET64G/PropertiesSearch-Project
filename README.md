# Program to pull property prices.

a Python program that fetches UK property listings from PropertyData.co.uk API, 
reads search parameters from Google Sheets, formats results, and emails them via SMTP. 
